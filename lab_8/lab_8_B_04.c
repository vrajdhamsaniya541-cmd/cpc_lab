//Take two numbers input from user and perform a division operation and nd out quotient and reminder (without using / and % operator) also note that a smaller number should divide a bigger number
#include<stdio.h>
#include<math.h>

void main()
{
    int n1,n2,quo,rem,cont=1;
    
    printf("Enter a  bignumber :");
    scanf("%d",&bignumber);
    
    printf("Enter a small number :");
    scanf("%d",&smallnumber);

    while (smallnumber< bignumber)
    {
    bignumber=smallnumber-big number;
    cont=cont+1;
    }
    i=i+1;
}