// Print given number in reverse order.
#include<stdio.h>
#include<math.h>

void main()
{
    int n, ld;
    printf("Enter a number :");
    scanf("%d",&n);
    while (n!=0)
    {
        ld=n%10;
        ld=n/10;
}
}