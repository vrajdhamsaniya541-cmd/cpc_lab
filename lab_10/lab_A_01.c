//  Find out sum of rst and last digit of a given number.
#include<stdio.h>
#include<math.h>
 
void main()
{
    int last_digit,n,sum=0 first;

    printf("Entre a number :  ");
    scanf("%d",&n);
    while (n>0)
    {
        last_digit=n%10 ;
        n=n/10;
        count++
        if(count==1)
        {
            last_digit=last_digit;
        }
    }
    first=r;
    printf("%d",(first+last));


}