// Print digits of given number.
#include<stdio.h>
#include<math.h>

void main()
{
    int n, ld;
    printf("Enter a number :");
    scanf("%d",&n);
    while (n!=0)
    {
        ld=n%10;
        printf("id",&n);
        ld=n/10;
    }
    
}